[package]
name = "example_agent"
version = "0.1.0"
description = "Chatbot Agent with full agentic loop, MCP tool use, and LLM inference"
authors = ["Tagentacle Contributors"]
type = "executable"

[entry_points]
node = "client:main"

[dependencies]
python = ["mcp", "anyio"]
